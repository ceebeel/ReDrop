[workspace]
members = ["src/common", "src/redrop", "src/player"]
default-members = ["src/redrop"]
resolver = "2"

[profile.release]
opt-level = "z"
codegen-units = 1
lto = "fat"
panic = "abort"
strip = "symbols"
